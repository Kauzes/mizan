server:
  port: 8086

spring:
  application:
    name: bank-simulator

springdoc:
  # Keys in a stable order, so a regenerated spec differs only where the API differs and the
  # committed file stays reviewable.
  writer-with-order-by-keys: true

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  endpoint:
    health:
      probes:
        enabled: true
      group:
        # A stand-in for an acquirer, with nothing behind it. Said explicitly rather than
        # left to the default, so the next service to be added has to answer the question
        # rather than inherit an answer.
        readiness:
          include: readinessState
  metrics:
    tags:
      service: ${spring.application.name}

logging:
  pattern:
    correlation: "[%X{correlationId:-}] "
