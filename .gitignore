# Build output
build/
out/
bin/
target/
*.class
*.jar
# Every Gradle wrapper, not only the root one: without the wildcard this line is anchored to the
# repository root, and android/'s wrapper would be silently left out of every commit.
!**/gradle/wrapper/gradle-wrapper.jar

# Gradle
.gradle/
gradle-app.setting

# Node
node_modules/
dist/
.vite/
*.local

# Android
*.apk
*.aab
local.properties
.cxx/

# IDE
.idea/
*.iml
.vscode/
*.swp

# Environment and secrets. .env is committed deliberately: it pins image tags, not secrets,
# and both Compose and the test harness read it.
!.env
.env.local
.env.*.local
*.pem
*.p12

# Logs and local data
*.log
logs/
pgdata/
kafka-data/

# OS
.DS_Store
Thumbs.db
