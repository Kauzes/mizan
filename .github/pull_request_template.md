## What

Closes MIZ-NN.

One paragraph on what changed and why.

## Checklist

- [ ] Tests cover the new behaviour, including the failure path
- [ ] OpenAPI spec updated if an endpoint changed
- [ ] `./gradlew build` passes
- [ ] `docker compose up` still reaches a healthy state
- [ ] ADR added if a decision an interviewer could challenge was made
